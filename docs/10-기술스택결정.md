# 10. 기술 스택 결정

2026-09-22 확정 · 기준 문서: `메일_쿠폰_알림_앱_기술_아키텍처_설계.md` "백엔드 기술 스택(Node.js) 및 인프라 구성"

## 이 문서를 만든 이유

원본 설계문서와 `05-백엔드아키텍처.md`, `06-모바일앱구조.md`는 기술 스택을 **제안** 형태로만 적어두었다("Node.js + NestJS(또는 Express)", "추천 기술: React Native", "ECS Fargate 또는 K8s", "RDS/Cloud SQL"). 그런데 `PROGRESS.md`의 체크리스트는 그 제안들을 확정 사항처럼 옮겨 적어, 실제로 결정된 것은 **Node.js 하나뿐**이라는 사실이 가려져 있었다.

`00-개요.md`의 "착수 전 결정 사항" 5건에도 스택 선택은 포함되어 있지 않았다. 즉 이 항목은 결정 안건으로 올라간 적이 없다. 1단계(MVP 백엔드)의 모든 인프라 작업이 여기에 걸려 있으므로 착수 전에 확정한다.

## 결정 전제

| 전제 | 출처 | 스택 선택에 미치는 영향 |
| --- | --- | --- |
| 베타 사용자 100명 이내 | `09-배포및로드맵.md` (Gmail unverified 앱 제한) | 대규모 오케스트레이션·APM 불필요 |
| 1단계 1~2주, 2단계 2~3주 | `09-배포및로드맵.md` 로드맵 표 | 초기 설정 비용이 큰 선택지 배제 |
| 사실상 1인 프로젝트 | 발신자 집계를 본인·지인 계정으로 수행 (`02-쿠폰판별로직.md`) | 운영 부담 최소화가 최우선 |
| **개발은 Claude Code에 전면 위임** | 2026-09-22 결정 | 러닝커브보다 *일관된 코드 생성*과 *수동 콘솔 조작 최소화*가 중요 |
| **macOS 미보유** | 2026-09-22 확인 | iOS 빌드는 클라우드 빌드만 가능 → Expo/EAS가 사실상 강제 |
| CASA 비용 미확정 | `07-보안개인정보.md`, `09-배포및로드맵.md` | 인프라 고정비를 낮게 유지할 이유 |
| 메일 원문 영구 미저장 | 원본 설계 "성공 기준" | 스토리지 요구 작음 |

원본 설계가 제안한 K8s/ECS, Datadog, 멀티 클라우드는 위 전제에 비해 과잉이므로 채택하지 않는다.

## 확정 스택

| 계층 | 확정 | 원본 설계 제안 | 변경 여부 |
| --- | --- | --- | --- |
| 클라우드 | **GCP 단독** | AWS / GCP 병기 | 변경 |
| 실행 환경 | **Cloud Run** (API 1 + 워커 1) + **Cloud Run Jobs** (배치) | Docker + K8s 또는 AWS ECS | 변경 |
| 큐 | **Pub/Sub** (메일 수집) + **Cloud Tasks** (발송·리마인더 예약) | Redis + BullMQ | 변경 |
| 데이터베이스 | **Cloud SQL (PostgreSQL)** | PostgreSQL (RDS/Cloud SQL) | 유지 |
| 웹 프레임워크 | **NestJS + TypeScript** | NestJS 또는 Express | 확정 |
| ORM·마이그레이션 | **Prisma** | (없음) | 신규 |
| 모바일 | **React Native + Expo** (development build, EAS Build) | React Native | 구체화 |
| 비밀 관리 | **GCP Secret Manager + Cloud KMS** | AWS Secrets Manager / GCP Secret Manager | 확정 |
| 모니터링 | **Cloud Logging/Monitoring + Sentry 무료 티어** | Sentry + Datadog/Grafana | 변경 |
| 인프라 관리 | **Terraform (IaC)** | (없음) | 신규 |
| 환경 | **dev + prod 2개** (staging은 3단계 진입 시 추가) | dev/staging/production 3개 | 변경 |

## 항목별 근거

### 클라우드 — GCP 단독

Gmail watch 알림은 반드시 GCP Pub/Sub 토픽으로 수신되고, OAuth 동의화면·심사와 FCM도 모두 GCP/Firebase다. GCP는 어차피 회피할 수 없다. 여기에 AWS를 더하면 1인 운영에서 계정·IAM·네트워크 경계가 두 벌이 된다.

### 실행 환경 — Cloud Run

베타 트래픽은 사실상 0에 가깝다. Cloud Run은 scale-to-zero로 유휴 비용이 없고, Gmail webhook 수신에 필요한 HTTPS 엔드포인트가 자동으로 붙는다. GKE는 클러스터 고정비와 운영 부담이 이 규모에 맞지 않는다.

Pub/Sub push 구독과 Cloud Tasks는 모두 **HTTP POST로 작업을 전달**하므로, 워커도 상주 프로세스가 아니라 요청 기반 서비스가 된다. 따라서 API와 워커 모두 `min-instances=0`으로 두어 유휴 비용이 없다. 대가는 유휴 후 첫 요청의 콜드 스타트인데, 성공 기준이 "수 분 이내 알림"이므로 허용 범위다. 배치(watch 갱신, 6시간 보정 폴링)는 Cloud Scheduler가 Cloud Run Jobs를 호출하는 형태로 구성한다.

### 큐 — Redis + BullMQ 대신 Pub/Sub + Cloud Tasks

원본 설계의 Redis + BullMQ는 서버리스 실행 환경과 궁합이 나쁘다. GCP 관리형 Redis(Memorystore)는 최소 인스턴스에도 월 고정비가 붙고 VPC 커넥터가 필요해, Cloud Run의 scale-to-zero 이점을 상쇄한다.

반면 Gmail watch가 **이미 Pub/Sub를 사용**하므로 수집 큐로 재사용하면 추가 인프라가 없다. Cloud Tasks는 지연 예약과 재시도가 내장되어 있어 만료 임박 리마인더에 그대로 맞는다. 둘 다 이 규모에서는 무료 티어 범위다.

**대가**: BullMQ의 대시보드(Bull Board)와 로컬 개발 편의를 잃는다. 로컬에서는 Pub/Sub 에뮬레이터를 사용한다.

### 웹 프레임워크 — NestJS

개발을 전면 위임하므로 러닝커브는 선택 기준에서 빠진다. 대신 두 가지가 중요해진다.

1. `05-백엔드아키텍처.md`의 모듈/폴더 구조가 이미 NestJS 전제로 작성되어 있다 — 다른 선택은 문서 재작성을 유발한다.
2. NestJS는 데코레이터·DI·모듈 규약이 강해 세션이 나뉘어도 코드 스타일이 일관되게 유지된다. 규약이 약한 프레임워크는 작업 세션마다 구조가 흔들리기 쉽다.

`AuthService` / `MailIngestService` / `CouponClassifierService` / `NotificationService` / `UserService` 5개 모듈 분리에도 잘 맞는다.

**대가**: 사용자가 프레임워크를 모르는 상태이므로 유지보수가 에이전트에 의존하게 된다. 이는 전면 위임 결정에 이미 내포된 사항이다.

### 데이터베이스 — Cloud SQL

Neon·Supabase 같은 서버리스 PostgreSQL은 무료 티어와 scale-to-zero가 매력적이지만, **암호화된 Gmail refresh token이 제3자 인프라에 저장**된다. CASA 백서에 하위 처리자를 별도로 기술해야 하고 심사 설명이 복잡해진다. 얻는 비용 절감보다 심사 리스크가 크다.

`03-API-DB-스펙.md`의 스키마가 PostgreSQL 문법으로 작성되어 있으므로 DB 엔진 자체는 유지한다.

### 모바일 — Expo (선택이 아닌 요구사항)

macOS가 없으면 로컬에서 iOS 빌드를 만들 수 없다. **EAS Build(클라우드 빌드)가 유일한 경로**이므로 Expo를 사용한다. 현재 요구 기능 — FCM 푸시, 딥링크, OAuth 웹뷰, 백그라운드 수신 — 은 모두 Expo development build 범위 안에 있어 bare workflow로 나갈 이유가 없다.

**대가**: EAS 무료 티어에는 월 빌드 횟수 제한이 있다. 빌드가 잦아지면 유료 전환이 필요하다.

### 인프라 관리 — Terraform

사용자가 GCP 콘솔을 직접 조작하지 않기로 했으므로, 인프라는 코드로 정의하고 사용자는 `terraform apply` 실행과 결과 확인만 한다. 콘솔 클릭으로 만든 리소스는 재현이 불가능해 dev/prod 이중 구성과 재해 복구에 불리하다.

### 환경 — dev + prod 2개

베타 100명 규모에서 3환경은 고정비가 3배가 된다. staging은 3단계(내부 베타) 진입 시 추가한다. 단 **Gmail OAuth 클라이언트 ID는 환경별로 분리**한다 (`05-백엔드아키텍처.md`) — 미검증 상태 테스트가 production에 영향을 주지 않도록.

## 비용 관점

고정비가 발생하는 항목은 **Cloud SQL 인스턴스 하나뿐이다.** Cloud Run은 요청이 없으면 0으로 내려가고, Pub/Sub, Cloud Tasks, Cloud Scheduler, FCM, Sentry 무료 티어, Cloud Logging은 이 트래픽 규모에서 무료 범위에 머문다.

> 구체적 금액은 리전·사양에 따라 편차가 크므로 **GCP 요금 계산기로 직접 확인한다**. 이 문서는 금액을 명시하지 않는다.

## 이 결정으로 갱신이 필요한 문서

- [x] `05-백엔드아키텍처.md` — 큐 설계를 Redis/BullMQ에서 Pub/Sub + Cloud Tasks로 재작성, 배포 섹션(ECS/K8s/ElastiCache)을 Cloud Run 기준으로 교체 (2026-09-22 완료)
- [x] `08-에러처리및엣지케이스.md` — 재시도 전략을 BullMQ 기준에서 Pub/Sub/Cloud Tasks 기준으로 조정 (2026-09-22 완료)
- [x] `03-API-DB-스펙.md` — DDL 적용 수단을 Prisma 마이그레이션으로 명시, 내부 메시지 스펙 교체 (2026-09-22 완료)
- [x] `메일_쿠폰_알림_앱_기술_아키텍처_설계.md` — 원본 baseline이므로 본문은 보존하고, 문서 상단과 스택·모바일 절에 대체 안내(superseded) 블록을 추가 (2026-09-22 완료)
- [x] `PROGRESS.md` — 1단계 인프라 항목을 확정 스택 기준으로 교체 (Redis/BullMQ 항목 제거, Terraform·Prisma 항목 추가) (2026-09-22 완료)

## 위임할 수 없는 작업 (사용자가 직접 수행)

개발은 위임하더라도 아래는 계정 소유자 본인이 해야 한다. 착수 전에 인지해둔다.

| 작업 | 사유 |
| --- | --- |
| GCP 계정 생성 및 결제 수단 등록 | 계정 생성·결제 정보 입력 |
| OAuth 동의화면 구성, 심사 제출 | Google 계정 소유자 행위 |
| Apple Developer Program 등록 (연 $99) | 결제 및 신원 확인. iOS 배포에 필수 |
| Google Play Console 등록 (1회 $25) | 결제 및 신원 확인 |
| Secret Manager에 실제 비밀값 입력 | 자격 증명 취급 |
| 개인정보처리방침·이용약관 법무 검토 및 게시 | 법적 책임 |
| CASA 심사 대상 여부·비용 확인 | 계정 단위 문의 |
| 발신자 집계용 Gmail 계정 접근 | 본인·지인 메일함 |
| 앱 서명 키 관리 | 자격 증명 취급 |

## 재검토 트리거

아래 상황이 발생하면 이 결정을 다시 연다.

- CASA 비용이 예상을 크게 초과해 프로젝트 규모 자체를 축소해야 할 때
- 정식 출시 후 사용자가 수천 명 규모를 넘어 Cloud Run 동시성·Cloud SQL 단일 인스턴스가 한계에 닿을 때
- MVP 이후 LLM 판별을 재도입할 때 (`02-쿠폰판별로직.md`) — 호출량에 따라 큐 구조 재검토 필요
